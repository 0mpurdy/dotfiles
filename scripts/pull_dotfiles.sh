dotpath="${HOME}/dev/dotfiles"

cp "${HOME}/.bashrc" "${dotpath}/nix"
cp "${HOME}/.vimrc" "${dotpath}/editor"
cp "${HOME}/.profile" "${dotpath}/nix"
cp "${HOME}/.config/nvim/init.vim" "${dotpath}/editor/nvim"
